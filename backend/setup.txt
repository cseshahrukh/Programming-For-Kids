pip install flask 
pip install flask_sqlalchemy
pip install psycopg2
pip install openai
npm install react-scripts


 #include <stdio.h>

int main() 
{
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d", a + b);
}
